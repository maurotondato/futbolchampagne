# Supabase (opcional para probar la app — sin esto arranca en "modo demo"
# guardando todo en el navegador). Los valores están en tu proyecto de
# Supabase > Project Settings > API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
